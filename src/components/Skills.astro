---
interface SkillCategory {
  title: string;
  icon: string;
  skills: { name: string; level: number }[];
}

const skillCategories: SkillCategory[] = [
  {
    title: "Frontend",
    icon: "🎨",
    skills: [
      { name: "React / Next.js", level: 90 },
      { name: "Vue.js", level: 85 },
      { name: "TypeScript", level: 88 },
      { name: "Tailwind CSS", level: 92 },
      { name: "Framer Motion", level: 75 },
    ],
  },
  {
    title: "Backend",
    icon: "⚙️",
    skills: [
      { name: "Laravel / PHP", level: 88 },
      { name: "Node.js / Express", level: 82 },
      { name: "Go", level: 70 },
      { name: "tRPC", level: 80 },
      { name: "REST APIs", level: 90 },
    ],
  },
  {
    title: "Database & Tools",
    icon: "🗄️",
    skills: [
      { name: "MySQL", level: 85 },
      { name: "Prisma ORM", level: 82 },
      { name: "Git / GitHub", level: 85 },
      { name: "Python", level: 78 },
      { name: "Docker", level: 65 },
    ],
  },
];
---

<section id="skills" class="py-24 px-6">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        Skills & <span class="text-gradient">Technologies</span>
      </h2>
      <p class="text-text-muted max-w-xl mx-auto">
        The languages, frameworks, and tools I use on a regular basis. I'm always picking up new ones, but these are my bread and butter.
      </p>
      <div class="w-20 h-1 bg-gradient-to-r from-primary to-accent rounded-full mx-auto mt-6"></div>
    </div>

    <div class="grid md:grid-cols-3 gap-8">
      {skillCategories.map((category) => (
        <div class="glass rounded-2xl p-6 glass-hover transition-all duration-300">
          <div class="flex items-center gap-3 mb-6">
            <span class="text-2xl">{category.icon}</span>
            <h3 class="text-lg font-bold">{category.title}</h3>
          </div>
          <div class="space-y-4">
            {category.skills.map((skill) => (
              <div>
                <div class="flex justify-between items-center mb-1.5">
                  <span class="text-sm text-text-muted">{skill.name}</span>
                  <span class="text-xs font-mono text-primary-light">{skill.level}%</span>
                </div>
                <div class="h-1.5 bg-surface-lighter rounded-full overflow-hidden">
                  <div
                    class="h-full bg-gradient-to-r from-primary to-accent rounded-full transition-all duration-1000"
                    style={`width: ${skill.level}%`}
                  />
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 flex flex-wrap justify-center gap-8 opacity-40">
      {["React", "Vue.js", "Next.js", "Laravel", "TypeScript", "Go", "Python", "MySQL", "Tailwind", "Prisma", "Node.js", "Astro"].map((tech) => (
        <span class="text-sm font-mono text-text-muted">{tech}</span>
      ))}
    </div>
  </div>
</section>
